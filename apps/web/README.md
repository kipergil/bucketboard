# @bucketboard/web

The Next.js (App Router) frontend for BucketBoard. See the [repository root README](../../README.md) for architecture, setup, and the full project overview.

```bash
pnpm --filter=./apps/web dev
```
